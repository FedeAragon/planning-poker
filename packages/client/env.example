# Lista separada por comas, en orden de preferencia.
# El cliente intenta conectar al primero y cae al siguiente si falla (~3s timeout).
# Ejemplo prod: VITE_SERVER_URLS="https://oracle.cfargotunnel.com,https://planning-poker-api.onrender.com"
VITE_SERVER_URLS="http://localhost:3001"
